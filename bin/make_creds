#!/bin/bash

read -p 'Username: ' username
read -sp 'Password: ' password
echo ""
read -sp 'Password confirmation: ' password2
echo ""
if [[ $password = $password2 ]]
then
    printf "${username}:`openssl passwd -apr1 ${password}`\n"
else
    echo "Password != confirmation"
fi
